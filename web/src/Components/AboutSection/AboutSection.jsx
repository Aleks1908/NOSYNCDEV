import React from "react";
import './about_section.css';

export const AboutSection = () => {
    return (
        <div className="about-main">
            {/* Create a react card component which will be reused for adding cards about facts*/}
            {/* Could be able to implement smth like the Jobs section without the making requests to the admin panel (hardcode in instead) */}
        </div>
    )
}
 
export default AboutSection;